<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/Styles.css">
  <title>Profile</title>
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #74ebd5, #acb6e5);
      color: #333;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }
    .container {
      background-color: #fff;
      padding: 40px;
      border-radius: 15px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
      text-align: center;
      width: 100%;
      max-width: 450px;
    }
    h2 {
      font-size: 28px;
      margin-bottom: 15px;
      color: #2c3e50;
      font-weight: 600;
    }
    p {
      font-size: 18px;
      color: #7f8c8d;
      margin-bottom: 30px;
    }
    a {
      display: inline-block;
      padding: 12px 25px;
      background-color: #3498db;
      color: white;
      text-decoration: none;
      border-radius: 30px;
      font-size: 16px;
      font-weight: 500;
      transition: all 0.4s ease;
    }
    a:hover {
      background-color: #2980b9;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>Akun sukses dibuat, <%= user.username %></h2>
    <p>Email: <%= user.email %></p>
    <p>Kelas: <script>document.write(document.cookie.split(';').filter(item => item.trim().startsWith('kelas=')).reduce((prev, current) => prev + current.split('=')[1], ''));</script></p>
    <a href="/auth/logout">Logout</a>
  </div>
</body>
</html>

